---
name: Raster Fotometria
description: Supported photometry options for the Raster page (RGB Palette default, RGB) — Uncompressed was removed from photometry
type: feature
---
On the Raster page, the photometry selector exposes only "RGB Palette" (default on page load) and "RGB". The "Uncompressed" option was removed from the photometry list per user request — it remains only as a compression option.
